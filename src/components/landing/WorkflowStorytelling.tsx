// Re-export from original location — the actual component remains in lims/
// but this file serves as the canonical import path for landing page usage.
export { default } from "../lims/GeologicalWorkflowStorytelling";
